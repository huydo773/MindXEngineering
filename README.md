# Containerized API on Azure

## Overview
This project demonstrates a **simple containerized API** deployed to **Azure Web App for Containers**, with the Docker image stored in **Azure Container Registry** and exposed via a **public HTTPS endpoint**.

## Technologies Used
- Docker  
- Azure Container Registry (ACR)  
- Azure Web App for Containers  
- REST API
